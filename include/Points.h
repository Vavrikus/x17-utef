#pragma once

#include "DataPoint.h"        // IWYU pragma: keep
#include "DriftLinePoint.h"   // IWYU pragma: keep
#include "EndPoint.h"         // IWYU pragma: keep
#include "EndPointDiscrete.h" // IWYU pragma: keep
#include "MapPoint.h"         // IWYU pragma: keep
#include "MicroPoint.h"       // IWYU pragma: keep
#include "RecoPoint.h"        // IWYU pragma: keep
#include "RKPoint.h"          // IWYU pragma: keep
#include "StartPoint.h"       // IWYU pragma: keep

namespace X17
{
  struct DataPoint;
  struct DriftLinePoint;
  struct EndPoint;
  struct EndPointDiscrete;
  struct MapPoint;
  struct MicroPoint;
  struct RecoPoint;
  struct RKPoint;
  struct StartPoint;
} // namespace X17